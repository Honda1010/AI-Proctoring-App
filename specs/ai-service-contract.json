{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "DetectionEvent",
  "description": "Normalized schema for AI service detection events",
  "type": "object",
  "required": [
    "service",
    "timestamp",
    "confidence",
    "sessionId",
    "payload"
  ],
  "properties": {
    "service": {
      "type": "string",
      "enum": [
        "eye-gaze",
        "object-detection",
        "face-recognition",
        "face-detection",
        "speech-detection"
      ],
      "description": "Name of the AI service emitting the event"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 UTC timestamp of the detection"
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Confidence score of the detection (0.0 to 1.0)"
    },
    "sessionId": {
      "type": "string",
      "description": "Unique identifier for the current exam session"
    },
    "payload": {
      "type": "object",
      "description": "Service-specific detection data",
      "additionalProperties": true
    }
  }
}
